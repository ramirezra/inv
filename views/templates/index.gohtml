<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="views/css/main.css">
  <title>Inventory</title>
  </head>
<body>
  <h1>CAM RPC Lead Management</h1>
  {{ range .}}
    <div class="lead-status">
      <section class="{{ .Status}}">
          <p>{{ .ID}}</p>
          <p>{{ .Status}}</p>
      </section>

    </div>
  {{ end}}

  {{ range .}}
  <p>
    <a href="/show?id={{.ID}}">{{.ID}}</a> - {{.Status}} - {{.Contact}} - {{.Sales}} - {{.Value}}
    <a href="/update?id={{.ID}}"> Update </a>
    <a href="/delete/process?id={{.ID}}"> Delete </a>
  </p>
  {{end}}

<hr>

  {{ template "table" .}}
  {{ template "card" .}}
</body>
</html>
